Duplicate File Detection System

This project detects duplicate files inside a folder using MD5 hashing.

Features

- Reads files from folder
- Generates hash values
- Compares files
- Detects duplicates

Technology Used

- Python
- OS module
- Hashlib module
